## HSQLDB
spring.datasource.url=jdbc:hsqldb:hsql://localhost/workdb


#drop and create table again, good for testing, (very) bad in production
spring.jpa.hibernate.ddl-auto=create

#skladnie sql i hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true










#####
# spring.data.rest.basePath=/api

# https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#boot-features-configure-datasource

# Hikari DBCP
# https://github.com/brettwooldridge/HikariCP